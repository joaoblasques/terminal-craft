# Manual-only installs

Apps with NO package-manager path at all — not a Homebrew formula, not a cask, not on
the Mac App Store (covered by `Brewfile` and `mas.txt` respectively). Add an entry here
the moment you notice one; this list is deliberately incomplete until then.

| App | Where to get it | Why |
|---|---|---|
